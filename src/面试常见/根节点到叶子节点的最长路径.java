/*
package 面试常见;

import Companies.BeiKe.Tree;
import ExperienceOfferTest.TreeNode;

import java.util.ArrayDeque;
import java.util.ArrayList;

public class 根节点到叶子节点的最长路径 {
 //   static int index = -1;
    static int max = Integer.MIN_VALUE;
    public static void main(String[] args) {
        ArrayList<ArrayList<TreeNode>> re = new ArrayList<>();
        ArrayDeque<TreeNode> deque = new ArrayDeque<>();
        TreeNode root = new TreeNode(1);
        deque.push(root);
        TreeNode res = func(root, re, deque);
        System.out.println();
    }

    public static TreeNode func(TreeNode root, ArrayList<ArrayList<TreeNode>> re, ArrayDeque<TreeNode> deque) {
        if (root == null) {
            if (deque.size() > max) {
                ArrayList<>
            }
        }
    }
}
*/
