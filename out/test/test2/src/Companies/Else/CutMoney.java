package Companies.Else;

import java.util.Scanner;

public class CutMoney {
    public static void main(String[] args){
        /*Scanner scanner = new Scanner(System.in);
        int n =scanner.nextInt();
        int[] arr = new int[n];
        for(int i = 0;i<n;i++){
            arr[i] = scanner.nextInt();
        }
        double max = 0;
        double total_left = arr[0];
        double total_right = arr[n-1];
        int left = 0;
        int right = n-1;
        while(left<right){
            while(left<right&&total_left<total_right){
                left++;
                total_left+=arr[left];
            }
            while(left<right&&total_left>total_right){
                right--;
                total_right+=arr[right];
            }
            if(total_left==total_right){
                max = Math.max(max,total_left);
                if(left<right-2){
                    left++;
                    total_left+=arr[left];
                }else{
                    break;
                }
            }
        }*/

        double a1 = 2;
        long a2 = 2;
        System.out.println(a1);
        System.out.println(a2);
    }
}
